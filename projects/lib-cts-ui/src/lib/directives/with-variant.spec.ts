import { WithVariant } from './with-variant';

describe('WithVariant', () => {
  it('should create an instance', () => {
    const directive = new WithVariant();
    expect(directive).toBeTruthy();
  });
});
