import { ZoomHover } from './zoom-hover';

describe('ZoomHover', () => {
  it('should create an instance', () => {
    const directive = new ZoomHover();
    expect(directive).toBeTruthy();
  });
});
