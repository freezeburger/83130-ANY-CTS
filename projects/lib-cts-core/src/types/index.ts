/**
 * Barrel file for types in the lib-cts-core project.
 * This file re-exports all types from the 'types' directory, making them accessible from a single entry point.
 * It allows for cleaner imports in other parts of the application, as you can import types directly from 'lib-cts-core' instead of specifying the path to each type file.
 */

export * from './generics.types';
export * from './crud.type';
