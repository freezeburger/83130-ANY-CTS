

## Decorateurs

> 19 

**Creation**

* `@NgModule`
  * `@Directive`
  * `@Component` ( Directive avec Template )
  * `@Pipe`
* `@Injectable`


**Communication**

* `@Input`              ->  `input()`     
* `@Output`             ->  `output()`
* `@ViewChild`          ->  `viewChild()`
* `@ViewChildren`       ->  `viewChildren()`
* ? `@ContentChild`     ->  `contentChild()`
* ? `@ContentChildren`  ->  `contentChildren()`

* `@HostBinding`        ->  Utilser `@Component` annotation
* ? `@HostListener`     ->  Utilser `@Component` annotation
* ? `@Attributes`       ->  Utilser `@Component` annotation

**Injection**

* `@Inject`         ->  `inject()`
* `@Host`           ->  Options de `inject()`
* `@Self`           ->  Options de `inject()`
* `@SkipSelf`       ->  Options de `inject()`
* `@Optional`       ->  Options de `inject()`



